@model NgControlGroup

<div class="form-group col-md-@Model.Size @Model.GroupCssClass" @Html.Raw(Model.Attributes)>
    <label>@Html.Raw(Model.Label)@((Model.IsRequired) ? " *" : "")</label>
    @Model.InputControl
    
</div>
