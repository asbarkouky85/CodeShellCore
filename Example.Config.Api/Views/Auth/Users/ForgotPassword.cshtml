@model ResetPasswordDTO
@{
    Layout = Html.Config().Layout;
}
@using (Html.NgForm("Form"))
{
    @Html.ControlGroup(e => e.Email, coll: Html.VCollection().AddRequired().AddEmail())
}
@section buttons{
    @Html.SubmitButton()
}